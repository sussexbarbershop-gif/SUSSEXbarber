# NOTE: admin/ is deployed on purpose — the shop owner works from it.
# Its sign-in is currently client-side only, which is tolerable while the
# panel just edits localStorage (a break-in changes only the attacker's own
# browser). Once the panel writes to the shared Google Sheet, the password
# check MUST move server-side into the Apps Script.

# Source photos that are already duplicated into assets/
photo susex/
